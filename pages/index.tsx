import { Form } from "../components/form/Form";
import { Layout } from "../components/layout/Layout";

const Home = () => {
  return (
    <Layout>
      <Form />
    </Layout>
  );
};

export default Home;
